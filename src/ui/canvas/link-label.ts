import type { LinkKind } from "@/model/links/schema"

/** L'etichetta di ogni tipo, sul canvas e (con l'iniziale maiuscola) nel pannello. */
export const LINK_LABEL: Record<LinkKind, string> = { "maps-to": "mappa su" }
